[package]
name = "utils"
version = "0.1.0"
edition.workspace = true
license-file.workspace = true

[dependencies]
rand.workspace = true
serde.workspace = true
glam.workspace = true

[dev-dependencies]
# Seeded randomness for the tests: the repository rule is that tests which
# only need random draws use `openqmc::pcg::Rng`.
openqmc-rs = "0.2"
